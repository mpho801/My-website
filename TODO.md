# TODO - Bloomsoft HTML/CSS alignment

- [x] Add/ensure shared `style.css` is linked from pages.
- [x] Fix invalid HTML structure issues in `services.html` and `contact.html`.
- [x] Push/commit changes.
- [ ] Improve HTML structure consistency across pages (add missing `<main>`, fix image alt text, wrap content for alignment).
- [ ] Improve CSS alignment/layout (center content, consistent spacing, set max-width and grid/flex for images/lists).
- [ ] Verify `index.html`, `about.html`, `services.html`, `enquiry.html`, `contact.html` all render consistently.

